import 'package:flutter/material.dart';

class playPage extends StatelessWidget {
  const playPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("PlayPage"));
  }
}
